version: '3.8'
services:
  s3mer:
    container_name: kino
    restart: always
    image: ghcr.io/wubanga/kino/:${IMAGE_VERSION}
    ports:
      - 5000:5000
    labels: 
      - traefik.http.routers.s3mer.rule=Host(`kino.okinazuno.me`)

networks:
  default:
    external: true
    name: traefik-network